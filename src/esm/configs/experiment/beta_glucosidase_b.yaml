# @package _global_
experiment:
  protein_dir_name: beta-glucosidase B  (In Silico_ Supervised)

model:
  input_dim: 445
  hidden_size1: 8

data:
  label: expression
  batch_size: 32

model:
  input_dim: 1280

trainer:
  max_epoch: 1000 