# @package _global_
experiment:
  protein_dir_name: Alpha-Amylase (In Silico_ Supervised)

model:
  lr: 0.01
  pooling_mode: "flatten"
  input_dim: 545280

data:
  label: expression
  batch_size: 128
  use_bos: True

trainer:
  max_epoch: 400 