# @package _global_
experiment:
  protein_dir_name: Alpha-Amylase (In Silico_ Supervised)

model:
  input_dim: 544000
  lr: 0.01

data:
  label: specific activity
  batch_size: 128

trainer:
  max_epoch: 200